#include <stdio.h>

int main(void)
{
    int i, j;
    float x;

    scanf("%d%f%d", &i, &x, &j);

    printf("the value of i is: %d\n", i);
    printf("the value of x is: %f\n", x);
    printf("the value of j is: %d", j);

    return 0;
}
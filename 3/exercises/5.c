#include <stdio.h>

int main(void)
{
    int i;
    float x, y;

    scanf("%f%d%f", &x, &i, &y);

    printf("the value of x is: %f\n", x);
    printf("the value of i is: %d\n", i);
    printf("the value of y is: %f", y);

    return 0;
}